import { colors } from "@mui/material";

const appbar = {
  background: "none",
};

export const style = {
  appbar,
};

export const title = {
  display: {
    xs: "block",
    sm: "block",
    color: colors.common.black,
    paddingRight: "0.4rem",
  },
};
